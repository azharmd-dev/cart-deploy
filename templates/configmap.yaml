apiVersion: v1
kind: ConfigMap
metadata:
  name: cart 
  namespace: robomart
data:
  REDIS_HOST: {{ Values.configMap.REDIS_HOST }}
  CATALOGUE_HOST: {{ Values.configMap.CATALOGUE_HOST }}
  CATALOGUE_PORT: "8080"